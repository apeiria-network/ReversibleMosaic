# 可逆视觉打码 Android App 需求规格说明书（MVP V2）

> 文档版本：V2.0  
> 产品形态：Android APK  
> 主要技术方向：Python + Kivy/KivyMD + Buildozer  
> 目标版本：可交付 MVP  
> 文档日期：2026-07-24

## 1. 文档目的

本文档定义可逆视觉打码 Android App 的 MVP 产品范围、用户流程、算法边界、文件协议、Android 集成、非功能要求和验收标准，作为设计、开发、测试与交付的共同依据。

本文档中的规范词含义如下：

- **必须**：P0 强制要求，不满足即验收失败。
- **应当**：原则上必须满足；例外须形成书面技术决定。
- **可以**：可选实现，不影响 MVP 验收。
- **目标**：需测量并记录，是否作为发布阻断项以对应验收条款为准。

## 2. 产品定位与目标

本产品是在 Android 手机本地运行的单图可逆视觉打码工具。用户可将图片变换为明显扰乱的 PNG，也可使用一致的算法版本、轮数和分享代码恢复算法处理前的规范化像素。

MVP 目标：

1. 图片处理全程离线，不依赖服务器。
2. 支持一张图片的选择、预览、打码、恢复、保存和分享。
3. 对未经像素修改的本 App 打码输出，在参数一致时逐像素恢复规范化像素矩阵。
4. 兼容 App 已公开发布且仍受支持的历史算法版本，允许用户手动切换。
5. 交付可安装的签名 APK、工程源码、测试和构建文档。

本产品提供的是**可逆视觉混淆**，不是密码学意义上的加密，不得宣传为“无法破解”“军事级加密”或同类安全能力。

## 3. MVP 范围

### 3.1 P0 包含

- Android 单张图片处理。
- 静态 8 位 RGB/RGBA PNG 输入。
- 普通 8 位 RGB JPEG/JPG 输入。
- JPEG EXIF Orientation 1–8 修正。
- 2、5、15、30 轮处理。
- 默认分享代码和用户自定义分享代码。
- 当前及已发布历史算法版本的恢复兼容与手动切换。
- 无损 8 位 RGB/RGBA PNG 输出。
- Android 系统图片选择器、MediaStore 保存和系统分享。
- 本地教程、进度、取消和异常提示。

### 3.2 MVP 不包含

- 16 位 PNG。
- 灰度、灰度 Alpha、调色板 PNG 的直接格式保真；实现可安全展开为 RGB/RGBA，但不作为 P0 承诺。
- CMYK/YCCK JPEG、ICC 色彩管理一致性。
- WebP、GIF、动画图片、RAW 和其他未列格式。
- 批量、视频、云同步、账号、历史图片列表、分享代码托管或分享代码找回。
- 对截图、裁剪、缩放、滤镜、有损压缩、转码或像素编辑结果的无损恢复。
- 密码学机密性、真实性、完整性或抗暴力枚举保证。

### 3.3 后续优先级

| 优先级 | 能力 |
|---|---|
| P1 | 更广的 8 位 PNG 模式、轮数候选预览、可选文件完整性提示、增强无障碍体验、**可选色彩变换加强模式**（在纯位置置换之上叠加逐像素 RGB lifting，用于提升抗视觉恢复但保持逐字节可逆；MVP 默认关闭，UI 提供开关；关闭时颜色调色板保留，开启时输出彩色噪声但用户能选取"更强隐私模式"） |
| P2 | 16 位 PNG、CMYK/YCCK/IC方案 |

## 4. 术语与恢复边界

- **打码/加密**：使用确定性双射对像素进行视觉扰乱。UI 可使用“加密”，但必须伴随非强加密提示。
- **恢复/解密**：使用对应逆变换恢复规范化像素。
- **分享代码**：用户输入的 1–10 位十进制数字；它不是密码学密钥。
- **扰动种子**：分享代码去除前导零后得到的整数，范围为 0–9999999999。
- **派生参数**：由算法版本、图片规格和扰动种子按固定规则生成的置换、混合与扩散参数。
- **轮数**：连续执行完整单轮变换的次数，仅允许 2、5、15、30。
- **规范化像素**：应用 JPEG 方向修正并转换为 8 位 RGB/RGBA 后进入算法的矩阵。
- **原始打码输出 PNG**：本 App 保存后未发生像素、位深、尺寸或通道变化的 PNG。
- **辅助元数据**：PNG 文本块中的非敏感协议字段；可辅助选择参数，但不是恢复的可信根。

逐像素恢复仅指恢复规范化像素，不包括原文件字节、JPEG 压缩结构、EXIF、ICC、文件名或其他附加信息。

## 5. 数学与算法边界

设算法版本 `v`、图片规格 `s` 和扰动种子 `a` 确定单轮双射 `F(v,s,a)`。对规范化像素矩阵 `I` 和合法轮数 `n`，必须满足：

`(F⁻¹)ⁿ(Fⁿ(I)) = I`

算法必须遵守：

1. 仅使用定义明确的整数运算；8 位通道按模 256 运算。
2. 每个正向步骤都有严格逆步骤，逆向按相反顺序执行。
3. 不得使用均值块、缩小后放大、量化或有损编码作为核心变换。
4. 同一输入、版本、轮数和种子必须得到完全相同的输出。
5. 不得使用 Python 内置 `hash()` 或依赖平台、进程、线程调度的状态。
6. 分享代码只参与固定成本参数派生，不得改变用户选择的轮数、算法阶段或像素扫描次数，也不得按代码数值循环。
7. RGBA 的四通道在空间置换时共同移动；RGB 可参与可逆混合，Alpha 数值不得修改或与 RGB 混合。
8. 透明像素中的 RGB 必须保留；显示缩略图不得回写算法矩阵。
9. 边缘和小尺寸必须采用可逆规则，不得裁剪或以补值替代原像素。
10. 各已发布算法版本的行为和固定向量必须永久冻结；修改像素规则必须提升版本号。

**V1 定稿概述（2026-07-29）**：V1 采用**纯位置置换**（reversible spatial permutation），不做任何色彩变换。每轮扫描全部像素一次，通过 PRF 派生模 `(2R+1)^2` 邻域内的配对索引，仅当 `j > i` 时交换（canonical direction）。反演按索引倒序重放同一交换序列。**颜色调色板逐字节保留**——只是位置改变，值不改。半径 `R = max(8, min(W,H)//32)`，随图像尺寸自适应。RGBA 四通道整体作为像素单位移动，Alpha 从不被单独修改；透明像素中的 RGB 完整保留。可选色彩变换（P1 加强模式）代码保留在 [color_transform.py](../reversible_mosaic/core/algorithm/color_transform.py) 但**不接入 MVP 生产路径**。

**轮数集合修订（2026-07-29，二次修订）**：从最初的 `{1, 5, 10, 20}` 经 `{2, 5, 10, 20}` 定稿为 `{2, 5, 15, 30}`。定稿原因：单人视觉验收（[artifacts/visual_review/scorecard.md](artifacts/visual_review/scorecard.md)）显示原 10/20 轮档在少数内容丰富图（人脸/文字/明显轮廓）上仍能勉强辨认轮廓。加大主档和最高档轮次预算（10→15、20→30，各 +50%）以提升位移累积深度，2/5 轮 sanity 层保持不变。默认值仍为 5 轮。

## 6. 用户流程

### 6.1 打码流程

1. 用户从首页进入“打码”。
2. App 调用系统图片选择器，用户选择一张图片。
3. App 读取 URI，校验格式和资源限制，完成方向修正与像素规范化。
4. App 展示缩略图、宽高、格式、模式和文件大小。
5. 用户选择 2、5、15 或 30 轮，默认 5 轮。
6. 用户选择留空使用默认分享代码，或输入/生成分享代码。
7. App 在工作线程执行当前最新版算法并显示进度。
8. 完成后展示预览、耗时、分享代码提示、保存和分享操作。
9. 未保存结果或自定义分享代码时，离开结果页必须分别提示可能丢失。
10. 保存通过 MediaStore 新建 PNG，不覆盖输入。

### 6.2 恢复流程

1. 用户从首页进入“恢复”并选择一张图片。
2. App 校验输入并解析辅助元数据。
3. 页面始终显示可操作的算法版本下拉框、轮数选项和分享代码输入框。
4. 有合法版本/轮数元数据时自动带入，但不得锁定控件；用户可改选任一受支持版本和轮数。
5. 版本元数据缺失或异常时默认最新版并提示可尝试历史版本；轮数缺失或异常时默认 5 轮并要求用户确认。
6. 元数据声明未来或已不支持版本时，App 提示升级或不支持，不得冒充其他版本；用户仍可手动选择本机支持的版本尝试。
7. 用户确认算法版本、轮数和分享代码后，App 按所选参数执行恢复并展示结果预览。
8. 结果页持续显示“请根据预览确认恢复结果”，用户可保存、分享或修改参数重试。

### 6.3 教程流程

教程必须说明操作步骤、分享代码、版本和轮数匹配、传播限制、剪贴板风险、分享代码遗失风险以及非密码学安全边界。

## 7. 功能需求

### 7.1 首页与说明

| 编号 | 优先级 | 需求 |
|---|---|---|
| FR-HOME-001 | P0 | 首页必须提供“打码”“恢复”“教程”入口。 |
| FR-HOME-002 | P0 | 首页必须显示“所有图片仅在本机处理”。 |
| FR-HOME-003 | P0 | 首次启动必须展示可逆视觉混淆的技术边界，确认状态仅保存在本机。 |
| FR-HOME-004 | P0 | 主要触控区域不得小于 48dp，状态不得仅靠颜色表达。 |

### 7.2 图片选择与规范化

| 编号 | 优先级 | 需求 |
|---|---|---|
| FR-IMG-001 | P0 | 使用 Android Photo Picker 或 SAF，每次只选择一张图片。 |
| FR-IMG-002 | P0 | 输入必须通过 URI/流读取，不得依赖绝对文件路径。 |
| FR-IMG-003 | P0 | 支持静态 8 位 RGB/RGBA PNG 和普通 RGB JPEG/JPG；其他输入在处理前阻断并提示。 |
| FR-IMG-004 | P0 | JPEG 必须应用 EXIF Orientation 1–8，输出不保留非 1 的方向字段。 |
| FR-IMG-005 | P0 | 预览必须显示规范化宽高、输入格式、像素模式和文件大小。 |
| FR-IMG-006 | P0 | 文件不可访问、解析失败或资源超限时不得进入算法阶段。 |

### 7.3 打码

| 编号 | 优先级 | 需求 |
|---|---|---|
| FR-ENC-001 | P0 | 轮数仅允许 2、5、15、30，默认 5。 |
| FR-ENC-002 | P0 | 留空使用默认扰动种子 500000；用户输入仅接受 1–10 位十进制数字。 |
| FR-ENC-003 | P0 | 前导零按整数规范化；`000123` 与 `123` 算法等价，复制时使用规范化形式。 |
| FR-ENC-004 | P0 | 可使用安全随机源生成 100000–999999 的六位分享代码，并排除 500000。 |
| FR-ENC-005 | P0 | App 必须提供复制、清除和重新生成；随机生成值保持六位显示。 |
| FR-ENC-006 | P0 | 分享代码不得写入 PNG 元数据、像素标记、文件名、日志或自动分享文字。 |
| FR-ENC-007 | P0 | 复制分享代码时应标记敏感剪贴板（系统支持时）并提示剪贴板可能被其他软件读取。 |
| FR-ENC-008 | P0 | 打码使用当前最新版算法，结果保持规范化宽高、模式和 8 位通道精度。 |
| FR-ENC-009 | P0 | App 不保存分享代码，也不提供找回能力。 |

### 7.4 恢复与版本兼容

| 编号 | 优先级 | 需求 |
|---|---|---|
| FR-DEC-001 | P0 | 解密页始终提供算法版本下拉框，按“Vn（发布日期）”列出全部受支持版本，默认最新版。 |
| FR-DEC-002 | P0 | 有合法版本元数据时自动选中对应版本，但用户可随时改选并重新处理。 |
| FR-DEC-003 | P0 | 无合法版本元数据时保持最新版并显示非阻断提示，允许用户自选历史版本。 |
| FR-DEC-004 | P0 | 每个公开发布且仍受支持的算法版本必须保留严格对应的逆算法和固定向量。 |
| FR-DEC-005 | P0 | 轮数必须由用户在 2、5、15、30 中选择；合法元数据可自动带入，但不锁定控件。 |
| FR-DEC-006 | P0 | 分享代码留空使用 500000；非空输入规则与打码一致。 |
| FR-DEC-007 | P0 | App 必须使用用户确认的分享代码、算法版本和轮数执行恢复，并允许用户修改参数后重新处理。 |
| FR-DEC-008 | P0 | 正确版本、轮数和种子用于未修改的原始打码 PNG 时，必须逐像素恢复规范化矩阵。 |
| FR-DEC-009 | P0 | 参数错误、元数据缺失或无法校验均不得导致崩溃；结果页必须提示由用户判断。 |

### 7.5 保存、查看与分享

| 编号 | 优先级 | 需求 |
|---|---|---|
| FR-SAVE-001 | P0 | 打码和恢复结果必须通过 MediaStore 新建记录，不覆盖输入。 |
| FR-SAVE-002 | P0 | 默认文件名为 `RM_ENC_yyyyMMdd_HHmmss_Rn.png` 或 `RM_DEC_yyyyMMdd_HHmmss.png`；重名由系统安全处理。 |
| FR-SAVE-003 | P0 | 保存成功后保留本次输出 URI，提供“查看”“分享”“返回首页”。 |
| FR-SAVE-004 | P0 | 分享使用已保存 MediaStore URI；授予接收方临时只读权限，不生成包含分享代码的文字。 |
| FR-SAVE-005 | P0 | 分享前提示使用“文件/原图”发送，平台改动像素后不保证恢复。 |
| FR-SAVE-006 | P0 | 保存失败时不得公开半文件；当前进程和结果页存活期间保留结果以供重试。 |
| FR-SAVE-007 | P0 | 未保存结果离开页面时必须二次确认，并明确离开后结果可能丢失。 |

### 7.6 任务、状态与历史

| 编号 | 优先级 | 需求 |
|---|---|---|
| FR-TASK-001 | P0 | 解码、算法和编码不得阻塞 UI 主线程；UI、纹理和 Intent 操作回到主线程。 |
| FR-TASK-002 | P0 | 处理中显示阶段及确定或不确定进度，并阻止重复启动。 |
| FR-TASK-003 | P0 | 取消采用协作式检查点；接到取消后不得开始新的算法轮次或保存操作。 |
| FR-TASK-004 | P0 | 任务状态采用：已选图、已规范化、处理中、预览就绪、保存中、已提交/失败/取消。 |
| FR-TASK-005 | P0 | MVP 可锁定竖屏；切后台不承诺任务继续，返回后应恢复安全状态或提示重新处理。 |
| FR-TASK-006 | P0 | 进程被系统终止后不恢复处理中任务；下次启动清理私有临时数据和孤立待提交项。 |
| FR-TASK-007 | P0 | App 不保存图片处理历史、最近图片 URI 或分享代码；可以保存非敏感偏好。 |

### 7.7 提示等级

- **Error**：阻断继续，如格式不支持、资源超限、文件不可访问、算法版本不支持。
- **Warning**：允许继续，如元数据缺失、参数可能不匹配、图片可能被修改。
- **Info**：状态说明，如保存成功、本地处理、使用默认分享代码。

所有提示必须给出下一步操作，不能只显示错误编号。

## 8. 文件与元数据协议

### 8.1 输出像素

1. 打码输出为 8 位 RGB 或 RGBA 无损 PNG，宽高和通道数与规范化输入一致。
2. 恢复输出为恢复后的 8 位 RGB/RGBA PNG。
3. 编码器不得调色板化、降采样、预乘 Alpha 或改写透明像素中的 RGB。

### 8.2 元数据载体

辅助元数据使用单个未压缩 PNG `tEXt` 块，关键字固定为 `reversible_mosaic`，值为仅含 ASCII 字符的紧凑 JSON 对象；不得使用嵌套对象或数组。

| 字段 | 类型/取值 | 必需 | 说明 |
|---|---|---|---|
| `schema_version` | 整数 `1` | 是 | 元数据协议版本 |
| `app_marker` | 字符串 `reversible_mosaic` | 是 | App 输出标识 |
| `operation_type` | `encrypted` / `restored` | 是 | 区分打码和恢复输出 |
| `algorithm_version` | 正整数 | 打码输出是 | 使用的算法版本 |
| `rounds` | `2/5/15/30` | 打码输出是 | 处理轮数 |
| `pixel_mode` | `RGB/RGBA` | 是 | 规范化模式 |
| `width` / `height` | 正整数 | 是 | 规范化尺寸 |

### 8.3 元数据安全规则

1. JSON 序列化必须使用确定的字段名和十进制整数，不解释为路径、格式串或代码。
2. 单块值最长 2048 字节；最多读取 4 个同名候选块；全部 PNG 文本元数据累计读取上限 64 KiB。
3. 同名块重复、字段类型错误、必需字段缺失或 schema 不支持时，该辅助元数据整体无效。
4. 未知字段忽略；实际 PNG 模式、宽高与元数据冲突时，以实际像素为准并将元数据标为异常。
5. 恢复输出必须使用 `operation_type=restored`；其中 `algorithm_version` 和 `rounds` 记录本次恢复所选参数，不能证明结果正确。恢复入口检测到该类型时必须警告“这是恢复输出，不是待恢复密文”，但允许用户继续尝试。
6. 元数据不得包含分享代码、扰动种子、派生参数、原图/密文摘要、缩略图、路径、身份信息或是否使用自定义分享代码的标记。
7. 删除全部元数据后，用户使用正确版本、轮数和分享代码仍必须恢复未修改像素。

## 9. Android 系统集成

### 9.1 输入 URI

1. 优先使用系统 Photo Picker；不可用时使用 SAF。
2. `content://` URI 必须通过 `ContentResolver` 流读取，不尝试解析“真实路径”。
3. 若任务不能在授权有效期内完成，应先复制到 App 私有缓存；复制也必须执行文件大小和磁盘预算检查。
4. 输入流和文件描述符在成功、失败及取消路径均必须关闭。

### 9.2 MediaStore 事务

1. Android 10 及以上保存时使用 `DISPLAY_NAME`、`MIME_TYPE=image/png`、`RELATIVE_PATH` 和 `IS_PENDING=1`。
2. PNG 编码和基本复读校验成功后将 `IS_PENDING` 置为 0。
3. 编码、校验、取消或提交失败时删除 pending 记录；下次启动清理本 App 创建且可确认未提交的孤立记录。
4. 最低 Android 版本及旧版保存策略必须在构建基线冻结后记录于构建文档。
5. App 仅申请选图、保存和分享所必需的最小权限，不申请网络、位置、联系人或麦克风权限。
6. MVP 最低系统版本为 Android 8.0（API 26），P0 CPU 架构为 `arm64-v8a`；其他 ABI 不属于首版验收范围。
7. `targetSdk` 必须满足发布渠道在正式发布日的要求，并在构建基线、测试报告和发行说明中记录确切值。
8. API 26–28 使用兼容的 MediaStore 插入与媒体扫描流程；写入失败时删除目标，不得依赖 Android 10 的 `IS_PENDING`。

## 10. 资源、安全与可靠性基线

### 10.1 MVP 初始限制

| 项目 | P0 阈值 | 超限行为 |
|---|---:|---|
| 输入文件可读取字节数 | 50 MiB | 处理前拒绝 |
| 最大边长 | 8192 像素 | 读取头部后拒绝 |
| 最大总像素 | 20,000,000 | 解码前拒绝 |
| 极端宽高比 | 最高 64:1 | 拒绝 |
| 单个 ICC/EXIF 段读取量 | 1 MiB | 忽略或拒绝异常输入，不无界读取 |
| PNG 文本元数据累计读取量 | 64 KiB | 忽略辅助元数据，仍按像素安全评估 |
| 私有缓存预留 | 预计输入复制与输出之和再加 20% | 空间不足时拒绝 |
| 算法全分辨率缓冲区 | 最多 3 份等尺寸像素矩阵 | 超预算时拒绝 |
| 取消响应目标 | 当前原子步骤结束后 2 秒内 | 超出需记录缺陷 |

以上阈值作为 MVP 初始值；修改必须同步需求版本、测试用例和发行说明，不得由不同设备静默采用互相矛盾的固定阈值。内存预算还必须结合运行时可用内存，预计峰值超过可用应用内存 60% 时提前拒绝。

**总像素上限 2026-07-29 修订**：原初始值 12,000,000 会拒绝几乎所有现代手机相机的默认输出（iPhone/Pixel/主流安卓的 12 MP 输出通常 4032×3024 = 12.19 MP 或 4096×3072 = 12.58 MP），MVP 阶段调整为 20,000,000 以覆盖典型 12–20 MP 手机相机原图；对应内存预算 3 × 20M × 4 B = 240 MB 全分辨率缓冲 + 64 MiB 固定开销 ≈ 304 MB，仍在 8 GB RAM 中端 arm64 设备的舒适预算内。修订同步了 `reversible_mosaic/domain/limits.py::MAX_PIXELS` 与 `tests/unit/test_limits.py`。

### 10.2 性能目标

在约定的 8 GB RAM 中端 arm64 真机、签名 Release APK、1920×1080 8 位 RGB 图片上，排除首次启动并连续运行 5 次取中位数：

- 1 轮算法核心：不超过 3 秒。
- 15 轮端到端处理至预览：不超过 27 秒。
- 30 轮端到端处理至预览：不超过 52 秒。
- 峰值内存：不超过三份全分辨率像素矩阵、必要缩略图及 64 MiB 固定开销之和。

验收前必须在测试计划中记录设备型号、SoC、Android 版本、ABI、图片 SHA-256、冷/热状态、中位数和 P95。若约定设备尚未确定，AC-PERF 保持发布阻断，不能以“中端设备”口头判定通过。

### 10.3 安全失败

畸形签名、截断数据、CRC 错误、解压炸弹、异常 EXIF、超大声明尺寸和恶意元数据必须导致受控错误，不得造成崩溃、无界内存增长、路径穿越、代码执行、公开半文件或原图改写。

## 11. 威胁模型与隐私

### 11.1 保护目标

- 图片不离开设备，且核心功能在飞行模式可用。
- 分享代码不进入输出文件、日志、历史或分享文字。
- 输入异常时安全失败，不损坏原图或留下公开半文件。
- 普通查看者不能从打码结果直接辨认代表性测试图片的主要内容。

### 11.2 明确非目标

- 不抵御掌握算法的穷举、已知明文或选择明文分析。
- 不隐藏尺寸、通道数、文件大小或 App 标识。
- 不提供来源认证、抗篡改或恢复正确性证明。
- 不保证经社交平台、编辑器、截图或转码后的图片可恢复。
- 不防止操作系统截图、剪贴板历史或已获设备权限的软件读取屏幕内容。

### 11.3 隐私规则

1. 不集成广告、分析或非必要崩溃上传 SDK。
2. 日志不得记录图片内容、完整 URI、文件路径、分享代码或可识别元数据。
3. 临时文件仅存 App 私有缓存，成功、失败、取消及下次启动时清理过期内容。
4. 若未来增加遥测、网络或云功能，必须单独更新需求、权限和隐私政策。

## 12. 测试要求

### 12.1 固定测试集

发布测试集至少包含：

- 20 张普通 RGB PNG/JPEG，覆盖人像、文字、场景和插画。
- 8 张 RGBA PNG，包含半透明和 Alpha=0 下非零 RGB。
- 8 张合成边界图，覆盖 1×1、窄图、奇数尺寸、纯色、噪声和最大允许尺寸附近样本。
- 10 个异常样本，覆盖截断、伪造扩展名、超大声明、异常 EXIF 和元数据。

第三方样本必须记录来源、许可证和 SHA-256；合成脚本、依赖版本和固定种子纳入工程。

### 12.2 自动往返与兼容测试

1. RGB/RGBA 固定样本覆盖 2、5、15、30 轮。
2. 分享代码至少覆盖 0、1、500000、999999、9999999999、一个固定六位随机样本和前导零等价输入。
3. 对每个当前支持算法版本验证 `decrypt(encrypt(I,n,a),n,a) == I` 零差异。
4. 固定向量在 PC 和所有受支持 Android ABI 上必须一致。
5. 旧版本逆算法必须使用其发布时固定向量回归，不得因最新版重构改变结果。
6. 元数据存在、缺失、重复、超长、类型错误、未来 schema 和未来算法版本均须覆盖。
7. 验证分享代码不会出现在输出 PNG 字节可解析元数据、文件名、应用日志和分享文字中。

### 12.3 周期与视觉质量

因无法穷举所有图片和 100 亿个分享代码，MVP 采用“结构审查 + 分层抽样 + 人工复核”，不得据此宣称全部输入具有密码学安全性。

1. 详细设计必须审查置换、通道混合和扩散构造，排除明显恒等参数、无效置换及低阶抵消。
2. 对固定有效测试集和代表性分享代码检查 2–30 轮；2、5、15、30 轮结果不得等于原图。
3. 自动记录像素变化率、相邻像素相关性和边缘相似度；阈值由算法原型报告冻结后纳入本需求附录，未冻结前不得发布。
4. 从有效集选择 20 张内容丰富图片，由 3 名检查者独立评价 2、5、15、30 轮结果。
5. 每张至少 2 人认定主要人脸、文字或主体轮廓难以直接辨认方可通过；纯色、1×1、全透明等低信息图片仅验收可逆性，不验收视觉隐藏能力。
6. 使用不同代表性分享代码产生的结果必须具有可观察差异。如果同一个分享代码或同一类分享代码在至少 3 张内容丰富的测试图片上持续未通过已冻结的自动质量阈值或人工视觉验收，则视为参数派生规则存在系统性退化，该算法版本不得发布；纯色、1×1、全透明等低信息图片不参与此判定。

**MVP 单人验收偏差（2026-07-29 记录）**：本 MVP 由单一开发者完成，短期内无法组织 3 名独立检查者。经产品负责人（同一人）批准，视觉验收采用**单人 + 自动指标双重校验**替代 §12.3.4-5：单一检查者独立评分 80 组（20 图 × 4 轮），同时冻结的三项自动指标（像素变化率、相邻相关性、边缘相似度）必须全部通过阈值；两项均通过才能视为 §12.3.4-5 达成。此偏差**仅适用于 MVP 首个内部发布**；若产品面向公开用户或商业发布，必须重新组织 ≥3 名独立检查者按原条款重跑视觉验收，本条款自动失效。偏差记录在 [artifacts/visual_review_scorecard.md](artifacts/visual_review_scorecard.md) 的首部。

### 12.4 Android 与异常流程测试

- URI 选图、授权失败、切后台、取消、进程回收后的重新启动。
- MediaStore 保存、重名、空间不足、pending 回滚、查看与分享权限。
- 连续成功、连续失败和取消后继续处理正常图片。
- 飞行模式、Manifest 权限检查、系统字体缩放和深浅色模式。
- 至少一台低配置设备和一台约定性能设备上的签名 Release APK。

## 13. 验收标准

验收责任标记：

- **自动**：由自动化测试代码执行并生成报告；报告必须包含环境、输入和实际结果。
- **人工**：由产品验收人实际操作或观察后确认，不能仅以开发者自测代替。
- **联合**：自动化测试先验证可计算部分，产品验收人再完成真机操作、视觉判断或交付确认；两部分均通过才算通过。

| 编号 | 责任 | 验收条件 |
|---|---|---|
| AC-001 | 联合 | 自动完成构建、安装和启动冒烟测试；产品验收人确认签名 APK 可在 Android 8.0（API 26）及以上的 `arm64-v8a` 设备正常安装、启动和进入首页，`targetSdk` 满足发布渠道在正式发布日的要求。 |
| AC-002 | 人工 | 产品验收人逐项检查首页、打码、恢复、教程、提示和未保存退出行为是否符合 FR-HOME 与流程要求。 |
| AC-003 | 联合 | 自动测试支持及不支持的格式和单图限制；产品验收人通过系统选择器确认支持图片可预览、其他格式在算法前阻断且提示清楚。 |
| AC-004 | 自动 | 测试代码使用 Orientation 1–8 固定样本比较规范化像素，并验证输出 PNG 不依赖原方向字段显示。 |
| AC-005 | 自动 | 测试代码验证 2/5/15/30 轮、默认分享代码、合法与非法输入、随机分享代码范围和前导零规则。 |
| AC-006 | 自动 | 测试代码验证所有受支持算法版本的固定样本零差异往返和旧版本固定向量不漂移。 |
| AC-007 | 人工 | 产品验收人确认恢复页始终可切换受支持版本；有效元数据自动带入但不锁定，无效或缺失时默认最新版并提示。 |
| AC-008 | 自动 | 测试代码删除全部元数据后，使用正确版本、轮数和分享代码恢复未修改的原始打码像素，并进行逐像素比较。 |
| AC-009 | 人工 | 产品验收人确认可选择版本、轮数和分享代码执行恢复，并可根据预览修改参数重新处理。 |
| AC-010 | 自动 | 测试代码验证 PNG 元数据 schema、大小限制、异常处理及 `encrypted`/`restored` 区分符合第 8 节。 |
| AC-011 | 自动 | 测试代码扫描 PNG、文件名、日志、历史状态和自动分享文字，确认不包含分享代码且 App 无找回数据。 |
| AC-012 | 联合 | 自动覆盖 MediaStore 成功、失败和回滚路径；产品验收人确认结果在相册中可查看、可分享，且任何路径均不覆盖输入或留下可见半文件。 |
| AC-013 | 联合 | 自动执行并发、取消和状态恢复测试；产品验收人确认处理时 UI 可响应、可取消、不可重复启动，后台或失败后可安全重试。 |
| AC-014 | 自动 | 测试代码执行全部资源边界和恶意样本，确认安全失败且无崩溃、卡死、无界资源占用或任意文件访问。 |
| AC-015 | 联合 | 自动测试确认质量指标达到冻结阈值并完成周期检查；产品验收人组织 3 名检查者完成第 12.3 节视觉验收。 |
| AC-PERF | 自动 | 基准测试代码在约定设备和口径下采集耗时及峰值内存并生成报告，结果满足第 10.2 节；测试设备未冻结时不得放行发布。 |
| AC-016 | 联合 | 自动检查 Manifest 权限；产品验收人在飞行模式下完成选图、打码、恢复和保存流程。 |
| AC-017 | 人工 | 产品验收人按第 16 节清点源码、固定向量、测试脚本、样本清单、性能报告、构建文档和签名 APK。 |

## 14. 需求追踪矩阵

验收责任汇总：

- **代码自动完成**：AC-004、AC-005、AC-006、AC-008、AC-010、AC-011、AC-014、AC-PERF。
- **产品验收人手工完成**：AC-002、AC-007、AC-009、AC-017。
- **代码与产品验收人共同完成**：AC-001、AC-003、AC-012、AC-013、AC-015、AC-016。

| 需求域 | 主要需求 | 对应验收 | 测试方式 |
|---|---|---|---|
| 首页与说明 | FR-HOME-001～004 | AC-002 | UI/无障碍检查 |
| 输入与规范化 | FR-IMG-001～006 | AC-003、004、014 | 集成、边界、异常样本 |
| 打码与分享代码 | FR-ENC-001～009 | AC-005、011、015 | 单元、性质、日志/文件检查 |
| 恢复与版本 | FR-DEC-001～009 | AC-006～009 | 固定向量、元数据和 UI 测试 |
| 保存与分享 | FR-SAVE-001～007 | AC-012 | MediaStore/Intent 真机测试 |
| 任务与历史 | FR-TASK-001～007 | AC-013 | 并发、取消、生命周期测试 |
| 元数据协议 | 第 8 节 | AC-008、010、014 | 解析模糊测试与互操作测试 |
| 资源与性能 | 第 10 节 | AC-014、AC-PERF | 真机基准与内存监测 |
| 隐私安全 | 第 11 节 | AC-011、014、016 | 权限、日志、离线与恶意输入测试 |

## 15. 开发阶段与发布闸门

### 阶段一：算法与文件链路

- 冻结 V1 数学定义、参数派生、固定向量和质量阈值。
- 验证 RGB/RGBA、Alpha 隐藏 RGB、EXIF 方向与 PNG 往返。
- 建立历史算法注册表和每版本逆算法测试。

**闸门：** AC-006、AC-008 和 AC-015 的 PC 端部分通过后，才进入大规模 UI 开发。

### 阶段二：Android MVP

- 完成 Kivy 页面、URI 输入、工作线程、取消和 MediaStore 事务。
- 完成算法版本选择、元数据协议、教程和风险提示。

### 阶段三：稳定性与发布

- 执行真机性能、恶意输入、生命周期、权限与兼容测试。
- 固化 Python、Buildozer、python-for-android、SDK/NDK 和依赖版本。
- 生成并验证签名 Release APK。

## 16. 交付物

1. 完整 Python/Kivy 工程源码及依赖锁定文件。
2. 算法设计、版本注册规则和全部固定向量。
3. 自动化测试、性能测试、合成样本生成脚本和测试报告。
4. 测试集清单、来源、许可证和 SHA-256。
5. `buildozer.spec`、SDK/NDK 配置及 Windows/WSL/Linux 构建文档。
6. 可安装的签名 Release APK；签名密钥不进入源码仓库。
7. 用户教程、隐私说明、第三方许可和发行说明。

## 17. 后续版本原则

- 增加 16 位、CMYK/YCCK/ICC 或新输入模式前，必须先定义规范化矩阵与跨平台固定向量。
- 改变像素变换行为必须发布新算法版本，并保留已公开版本的逆算法。
- 改变元数据结构必须提升 `schema_version`，解析器对未知未来版本安全失败。
- 若未来增加密码学保护，必须采用标准密钥派生和认证加密；不得把现有分享代码机制重新命名为强加密。
- P1/P2 能力不得在未更新本需求、测试和验收标准前作为 MVP 已支持功能宣传。

## 18. 结论

MVP 是一款离线、单图、8 位、支持历史算法版本自选恢复的可逆视觉打码工具。其核心发布条件是：在正确算法版本、轮数和分享代码下，未修改的原始打码 PNG 可逐像素恢复规范化像素；代表性图片达到冻结后的视觉扰乱下限；Android URI、MediaStore、取消、资源限制和异常输入链路均能安全工作。产品范围以第 3 节为准，扩展能力应在满足核心验收条件的前提下按优先级实施。
